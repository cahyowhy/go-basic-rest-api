export default class TableColumn {
    
    public label: string = '';

    public field: string = '';

    public sortable: boolean = false;

    public centered: boolean = false;

    public width: string = 'auto';

    public customSlot: boolean = false;

    public isNumbering: boolean = false;
}
